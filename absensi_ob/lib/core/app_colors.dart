import 'package:flutter/material.dart';

/// Shared color palette — dipakai di LoginPage, RegisterPage, dll.
class AppColors {
  AppColors._();

  static const Color bg = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  static const Color primary = Color(0xFF4F6AF0);
  static const Color primaryLight = Color(0xFFEEF1FD);
  static const Color accent = Color(0xFF7C3AED);
  static const Color textDark = Color(0xFF1A1D2E);
  static const Color textMuted = Color(0xFF8B90A0);
  static const Color border = Color(0xFFE8EAF0);
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
}
